/// La enumeración del HttpMethod tiene cinco valores posibles.
/// Esta enumeración se puede utilizar para representar diferentes métodos de 
/// solicitud HTTP.

enum HttpMethod {
  get,
  post,
  put,
  patch,
  detele,
}
